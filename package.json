{
  "name": "deskreview-mistral-2",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "check:syntax": "node --check server/index.js && node --check public/app.js && node --check scripts/verify-current-baseline.mjs",
    "check:baseline": "node scripts/verify-current-baseline.mjs",
    "check": "npm run check:syntax && npm run check:baseline"
  }
}
